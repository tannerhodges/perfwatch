// const folderButtonEl = document.getElementById('folder-button');
// const folderPathEl = document.getElementById('folder-path');
// const fileChangesEl = document.getElementById('file-changes');

// folderButtonEl.addEventListener('click', async () => {
// 	const folderPath = await window.electronAPI.openFolder();
// 	folderPathEl.innerText = folderPath;
// });

// window.electronAPI.handleFileChange((event, path) => {
// 	fileChangesEl.insertAdjacentHTML('beforeend', `<li>${event}: ${path}</li>`);
// });
